TxScript StakeSix(
    amount: U256,
    contract: Stake
) {
    let minimalAlphInContract = 1 alph
    let approvedAlphAmount = minimalAlphInContract

    let token = contract.getMixId()

    contract.sixstake{callerAddress!() -> ALPH: approvedAlphAmount, token: amount}(amount)
}